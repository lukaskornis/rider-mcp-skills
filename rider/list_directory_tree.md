python list_directory_tree.py <directory_path> [max_depth] [project_path]
directory_path:str  project-relative path (e.g. "Assets" or "Assets/Core")
max_depth:int       recursion depth (default: full)
→ traversedDirectory tree:string (tree-formatted listing)
